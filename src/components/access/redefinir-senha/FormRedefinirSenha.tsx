export default function FormRedefinirSenha() {
  return <div>FormRedefinirSenha</div>;
  // campos de senha e confirmação de senha e um botão para envio a rota https://localhost:3000/auth/redefinir-senha que recebe uma senha e o token no body
}
